// const user:(string| number)[] = [1, "hc"]
let user1:[string, number, boolean]//must be in the same order
user1 = ["hc", 3, true]

let rgb : [number, number, number]
type User1 = [number, string]
const newUser: User1=[112, "etet"]


// tuples have fixed structure , better type safety and 